{
  "name": "basicmaxxer",
  "version": "1.0.0",
  "description": "Basic Maxxer - Personal Training Coach",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cap sync && cap build android --prod",
    "build:ios": "cap sync && cap build ios --prod"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
